name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on:ubuntu-latest
    steps:
      -name:print my name
       run:|
         echo "Shreekrupa"
         echo "GitHub"

      -name: Current Folder
       run:|
         pwd
      
         
