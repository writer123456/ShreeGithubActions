name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Print my name and GitHub
      run: |
        echo "Shreekrupa"
        echo "GitHub"

    - name: Current Folder
      run: |
        pwd
